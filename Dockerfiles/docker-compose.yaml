version: "3.9"

services:
  px4:
    container_name: px4_cont
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    image: px4
    runtime: nvidia
    network_mode: host
    ipc: host
    environment:
      DISPLAY: ${DISPLAY}
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: all
      QT_X11_NO_MITSHM: "1"
      XDG_RUNTIME_DIR: /tmp/runtime-root
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /dev/dri:/dev/dri
      - ../../PX4-Space-Systems:/home/px4space/PX4/PX4-Space-Systems
      #- ../../software_cobot:/home/px4space/softwareSpaceCobot
    stdin_open: true
    tty: true
